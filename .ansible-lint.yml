---
skip_list:
  - yaml[line-length]
  - name[casing]
  - risky-shell-pipe
  - var-naming[no-role-prefix]  # defaults vars are user-facing config, not internal role state

exclude_paths:
  - .github/
  - .ansible/
  - molecule/
  - .venv/
  - venv/

use_default_rules: true
verbosity: 1
