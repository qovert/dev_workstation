---
# macOS-only roles — only required when running against a macOS target.
# On Fedora/Linux these are never loaded and do not need to be installed.
roles:
  - name: elliotweiser.osx-command-line-tools
  - name: geerlingguy.homebrew
  - name: geerlingguy.mas

collections:
  - name: community.general
    version: ">=3.0.0"
  - name: geerlingguy.mac
    version: ">=3.0.0"
